import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/_authenticated/notificacoes/")({ component: () => <div className="p-4">Notificações em construção...</div> });
