import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/_authenticated/clientes/")({ component: () => <div className="p-4">Clientes em construção...</div> });
