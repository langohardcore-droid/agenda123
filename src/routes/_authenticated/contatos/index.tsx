import { createFileRoute } from "@tanstack/react-router";
export const Route = createFileRoute("/_authenticated/contatos/")({ component: () => <div className="p-4">Contatos em construção...</div> });
